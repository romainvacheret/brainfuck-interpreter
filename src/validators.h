#ifndef VALIDATORS_H
#define VALIDATORS_H

int is_valid_char(const char charact);
int is_valid_string(char* code_string);
int is_valid_code(char* code_string);
int are_brackets_valid(char* code_string);

#endif